% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt_extract.R
\name{opt_extract_landuse_grid}
\alias{opt_extract_landuse_grid}
\title{Extract land-use raster features with raster-aligned point chunks}
\usage{
opt_extract_landuse_grid(
  x,
  y,
  radius,
  id = c("gid", "year"),
  year = NULL,
  fun = "mean",
  workers = 1L,
  gdal_cache_mb = 2048L,
  max_cells_in_memory = 1e+08,
  start_supertile_px = 2048L,
  min_supertile_px = 1024L,
  safety_fraction = 0.85,
  enforce_chunk_memory_limit = FALSE,
  prefix = "landuse",
  rename = TRUE,
  chunk_dir = tempfile("huimori_landuse_chunks_"),
  cleanup = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{x}{A raster filename or \code{terra} raster object.}

\item{y}{An \code{sf} point object.}

\item{radius}{Numeric scalar. Buffer radius in meters.}

\item{id}{Character vector of identifier columns to keep in the result.}

\item{year}{Optional integer scalar. If supplied, a \code{year} column is added to
\code{y} before extraction.}

\item{fun}{Summary function passed to \code{\link[exactextractr:exact_extract]{exactextractr::exact_extract()}}.}

\item{workers}{Integer scalar. Number of future workers. Use \code{1} for
sequential extraction. Prefer \code{1} inside dynamic \code{targets} branches so the
pipeline scheduler controls parallelism across branches.}

\item{gdal_cache_mb}{Integer scalar. GDAL cache size in MB per process.}

\item{max_cells_in_memory}{Numeric scalar passed to
\code{\link[exactextractr:exact_extract]{exactextractr::exact_extract()}}.}

\item{start_supertile_px, min_supertile_px}{Integer chunk-size controls passed
to \code{\link[=opt_choose_supertile_px]{opt_choose_supertile_px()}}.}

\item{safety_fraction}{Numeric scalar passed to \code{\link[=opt_choose_supertile_px]{opt_choose_supertile_px()}}.}

\item{enforce_chunk_memory_limit}{Logical scalar passed to
\code{\link[=opt_choose_supertile_px]{opt_choose_supertile_px()}}.}

\item{prefix}{Character scalar used when renaming extracted value columns.}

\item{rename}{Logical scalar. If \code{TRUE}, rename extracted value columns as
\verb{\{prefix\}_\{column\}_\{radius\}}.}

\item{chunk_dir}{Optional directory for temporary chunk RDS files.}

\item{cleanup}{Logical scalar. Remove temporary chunk files on exit.}

\item{verbose}{Logical scalar. Print chunk information.}
}
\value{
A data frame containing \code{id} columns and extracted raster features.
}
\description{
Extract circular-buffer raster summaries for many point locations while
avoiding large future exports. Points are transformed to the raster CRS,
assigned to raster-aligned chunks, written to one temporary RDS file per
chunk, and processed in parallel by reading those chunk files inside workers.
}
\details{
This is intended for preprocessed land-use fraction rasters with
\code{fun = "mean"}, and can also be used with categorical rasters and
\code{fun = "frac"}.
}
