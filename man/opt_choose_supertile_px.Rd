% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/opt_extract.R
\name{opt_choose_supertile_px}
\alias{opt_choose_supertile_px}
\title{Choose a raster-aligned supertile size}
\usage{
opt_choose_supertile_px(
  r,
  max_radius_m,
  max_cells_in_memory = 1e+08,
  start_px = 2048L,
  min_px = 1024L,
  safety = 0.85,
  enforce_memory_limit = FALSE
)
}
\arguments{
\item{r}{A \code{terra} raster object or a raster filename.}

\item{max_radius_m}{Numeric scalar. Largest buffer radius in meters.}

\item{max_cells_in_memory}{Numeric scalar. Soft limit used when
\code{enforce_memory_limit} is \code{TRUE}.}

\item{start_px}{Integer scalar. Requested starting supertile width in pixels.}

\item{min_px}{Integer scalar. Minimum allowed supertile width in pixels.}

\item{safety}{Numeric scalar. Fraction of \code{max_cells_in_memory} to use as a
safety margin.}

\item{enforce_memory_limit}{Logical scalar. If \code{TRUE}, halve \code{start_px} until
the estimated padded cells fit under \code{max_cells_in_memory * safety}, without
going below \code{min_px}.}
}
\value{
A list with \code{supertile_px}, halo sizes, estimated padded cells, and
whether the raster is lon/lat.
}
\description{
Choose a square chunk size in raster pixels and estimate the buffer halo
needed around each chunk. When \code{r} is lon/lat, \code{max_radius_m} is converted
to approximate degrees before the halo is computed.
}
